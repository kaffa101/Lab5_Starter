1. It is import to put thought into your IDS and Classes because organization is important for good code
2. They are custom attributes you can add. They allow you to add extra info. YOu can access them with the dataset property. They help add meaning to elements.
3. They are containwers for DOM nodes. They are important for optimization.
4. They are an JS object represenation of the real DOM. You gain things like optimized performance, but lose things like memory overhead.
5. Class is a reserved name in JS.
6. onClick() is a HTML attribute and addEventListener is a JS method. addEventListener is more flexible and easier to use.